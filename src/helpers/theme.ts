import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    primary: {
      light: "#CECEFC",
      main: "#6D6DF8",
      dark: "#4848A5",
    },
  },
});

export default theme;